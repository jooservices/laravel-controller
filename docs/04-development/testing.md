# Testing

This package uses PHPUnit with Orchestra Testbench.

Run tests:

```bash
composer test
```

Run tests with coverage:

```bash
composer test:coverage
```

Coverage artifacts are generated under `build/coverage` and `build/coverage.xml`.